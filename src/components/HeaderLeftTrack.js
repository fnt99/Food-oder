import React from "react";
import { Text, View, Image, TouchableOpacity, StyleSheet } from "react-native";

const HeaderLeftTrack = ({ onPress, params }) => {
  return <View style={{ marginRight: 10 }}></View>;
};
export default HeaderLeftTrack;
